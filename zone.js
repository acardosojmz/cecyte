export class Zone{
    code;
    zone;
    costForSquareMeter;

    constructor(code, zone, costForSquareMeter) {
        this.code = code;
        this.zone = zone;
        this.costForSquareMeter = costForSquareMeter;
    }
}